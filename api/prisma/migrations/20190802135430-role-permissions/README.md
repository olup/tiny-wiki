# Migration `20190802135430-role-permissions`

This migration has been generated by loup topalian at 8/2/2019, 1:54:30 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."_users_can_view_pages"("A" TEXT NOT NULL  REFERENCES Page(id),"B" TEXT NOT NULL  REFERENCES Role(id));

CREATE TABLE "lift"."_users_can_edit_pages"("A" TEXT NOT NULL  REFERENCES Page(id),"B" TEXT NOT NULL  REFERENCES Role(id));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190801113707-dfg..20190802135430-role-permissions
--- datamodel.dml
+++ datamodel.dml
@@ -12,19 +12,22 @@
   provider = "nexus-prisma"
 }
 model User {
-  id    String  @default(cuid()) @id @unique
-  email String  @unique
-  name  String?
-  roles Role[]
+  id     String  @default(cuid()) @id @unique
+  email  String  @unique
+  name   String?
+  roles  Role[]
+  drafts Page[]  @relation(name: "draftOwner")
 }
 model Role {
-  id     String  @default(cuid()) @id @unique
-  users  User[]
-  slug   String  @unique
-  locked Boolean @default(false)
+  id        String  @default(cuid()) @id @unique
+  users     User[]
+  slug      String  @unique
+  locked    Boolean @default(false)
+  viewPages Page[]  @relation(name: "users_can_view_pages")
+  editPages Page[]  @relation(name: "users_can_edit_pages")
 }
 model Page {
@@ -32,6 +35,8 @@
   title      String
   slug       String  @unique
   content    String?
   published  Boolean
-  draftOwner User?
+  draftOwner User?   @relation(name: "draftOwner")
+  canView    Role[]  @relation(name: "users_can_view_pages")
+  canEdit    Role[]  @relation(name: "users_can_edit_pages")
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190802135430-role-permissions)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190802135430-role-permissions'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
